/* Executa uma função em todos os elementos do array, retornando um valor único.

    Descrição

    O método reduce() recebe dois argumentos: uma função de retorno de chamada e um valor inicial opcional. Se um valor inicial
    for fornecido, reduce() chama a função de retorno chamada "redutor" em cada elemento da matriz, em ordem. Se nenhum valor inicial 
    for fornecido, o reduce() chama a função de retorno de chamada em cada elemento matriz após o primeiro elemento.
*/

